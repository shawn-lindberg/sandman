services:
  sandman_web:
    build: ./sandman_web
    command: python manage.py run -h 0.0.0.0
    volumes:
      - $HOME/.sandman:/root/.sandman
      - "/etc/localtime:/etc/localtime:ro"
    ports:
      - 5001:5000
    env_file:
      - ./.env.dev